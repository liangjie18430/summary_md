# 为什么不同模型间可以蒸馏
不同的大模型间除了是数据的比例不一致外，底层语言的规范是一致的，还是有规律的，而大模型是从token间的前后共现关系学，所以还是可以蒸馏

# 为什么Next Token Predict就可以学好

虽然词表很大，而且每次的next token看起来是整个空间，如果词表空间大小为S, 预测一句话的长度为m,那么实际上的空间大小为S^m(即整体的组合数).但是前面每个token的Next token实际上可候选的是不多的。长度越长约束会越明显。

# 20260814
新增问题如下:

1、reward能否加在中间的token上，比如格式的reward，直接加在最后一个token上，会导致其他的token也获得格式reward的奖励。
2、sft中的输入mask的方式，如果将responses中一些错误的结果也mask，会发生什么，这样可以达到什么目的
3、多轮中的轮次mask，是如何实现的
4、基础过程的打分，本身也是类似1中将reward添加到中间的某个token上，而不是加在整个reponse输出的结尾上
5、优化优势函数的信用分配和优化reward之间会有什么差异，俩个分别适用什么场景
6、多轮对话的mask是如何实现的，假设一个n轮的对话
    * 比如第一轮对话，mask第一个用户的query，使用 assistant的response进行loss的计算
    * 第二轮呢，是使用第一轮的 query + response , 再加第二轮的query， 使用第二轮的assistant 进行loss的计算吗

